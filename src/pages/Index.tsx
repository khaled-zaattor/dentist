import { Layout } from "@/components/Layout";
import AdminDashboard from "./AdminDashboard";

const Index = () => {
  return (
    <Layout>
      <AdminDashboard />
    </Layout>
  );
};

export default Index;
